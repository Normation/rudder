name: CI

on: [push, pull_request]

jobs:
  test:
    name: test
    runs-on: windows-latest
    env:
      CI: 1

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@1.93.0

      - name: Run template module tests
        run: make cargo-test PACKAGE=rudder-module-template
      - name: Run schedule module tests
        run: make cargo-test PACKAGE=rudder-module-scheduler

    permissions:
      contents: read
      pull-requests: write
