@format=0

resource file(p)

file state permissions(u,g,m) { }

resource ntp ()

ntp state configuration ()
{
  file1("/tmp").permissions("root", "root", "g+w")
  file2("/tmp").permissions("root", "root", "g+w")
}
