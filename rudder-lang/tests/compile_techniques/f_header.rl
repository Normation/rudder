global enum os {
  debin # 1 ir1::enum_mapping()
}

items in debian {
  ubuntu
}

resource comman(p0) # 3 ir1::add_resources()
commandq state execution(){} # 3 ir1::add_resources()

resource Configure_NTP()

Configure_NP state technique() { # 3 ir1::add_resources()
  @component = "Package present"
  packae("ntp").preent("","","") as package_present_ntp # respectively 4 + 5 ir1::binding_check()
}
