# generated by rudder-lang
# @metadata 1

bundle agent Configure_NTP_technique
{
  vars:
    "resources_dir" string => "${this.promise_dirname}/resources";
  methods:
    "any_${report_data.directive_id}_0" usebundle => _method_reporting_context("any"),
                                               if => concat("ubuntu");
    "any_${report_data.directive_id}_0" usebundle => file_absent("/tmp"),
                                               if => concat("ubuntu");
    "any_${report_data.directive_id}_0" usebundle => _method_reporting_context("any"),
                                               if => concat("debian.!()");
    "any_${report_data.directive_id}_0" usebundle => file_present("/tmp"),
                                               if => concat("debian.!()");
      "method_call" usebundle => ncf_log("info: ok");
}
