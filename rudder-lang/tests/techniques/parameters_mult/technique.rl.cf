# @generated by rudderc
# @name parameters mult
# @version 1.0

bundle agent parameters_mult_technique(paramtest, 12) {
  vars:
    "resources_dir" string => "${this.promise_dirname}/resources";

  methods:
    "${report_data.directive_id}_0" usebundle => _method_reporting_context("Package absent", "vpn");
    "${report_data.directive_id}_1" usebundle => package_absent("vpn", "", "", "");
}