# Apache modules
APACHE_MODULES="${APACHE_MODULES} rewrite dav dav_fs proxy proxy_http headers ssl version filter deflate"

# Apache configuration flags
APACHE_SERVER_FLAGS="${APACHE_SERVER_FLAGS} SSL"
