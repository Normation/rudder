This directory contains a test suite to validate conventions and style guides in NCF.

The test framework will look in all sub-directories for files named *.{sh,py,pl} and run them, and make sure they return error code 0, and output a line "${filename} Pass".
