# TODO retrieve original cf technique 
# generated by rudderc
# @name technique_dsc
# @version 1.0
# @description 

bundle agent technique_dsc_technique {

  vars:
    "resources_dir"        string => "${this.promise_dirname}/resources";
    "args"                  slist => {};
    "report_param"         string => join("_", args);
    "full_class_prefix"    string => canonify("technique_dsc_technique_${report_param}");
    "class_prefix"         string => string_head("${full_class_prefix}", "1000");

  methods:
    # Directory check exists:
    # 
    #   directory("tmp").check_exists() as directory_check_exists_tmp
    # 
    "Directory check exists_${report_data.directive_id}_0" usebundle => _method_reporting_context("Directory check exists", "tmp");
    "Directory check exists_${report_data.directive_id}_0" usebundle => directory_check_exists("tmp");

    # Command execution:
    # 
    #   command("dsc").execution() as command_execution_dsc
    # 
    "Command execution_${report_data.directive_id}_1" usebundle => _method_reporting_context("Command execution", "dsc");
    "Command execution_${report_data.directive_id}_1" usebundle => command_execution("dsc");

}
