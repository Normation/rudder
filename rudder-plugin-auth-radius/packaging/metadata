{
  "type": "plugin",
  "name": "${plugin-id}",
  "version": "${plugin-version}",
  "build-date": "${maven.build.timestamp}",
  "build-commit": "${commit-id}",
  "jar-files": [ "/opt/rudder/share/plugins/auth-radius/auth-radius.jar" ],
  "content": {
    "files.txz": "/opt/rudder/share/plugins"
  }
}
