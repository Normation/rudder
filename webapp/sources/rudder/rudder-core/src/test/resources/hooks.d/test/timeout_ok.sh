#!/bin/bash

# this "HOOK_WARN_TIMEOUT" will not be used, neither the following, because of quotes:

# "HOOK_WARN_TIMEOUT" = 1ms

# check that it works with both space or not
# HOOK_WARN_TIMEOUT = 6 s
#HOOK_KILL_TIMEOUT = 10 s

sleep 2

exit 0
