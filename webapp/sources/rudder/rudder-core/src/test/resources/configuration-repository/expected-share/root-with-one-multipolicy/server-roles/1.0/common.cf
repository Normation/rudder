# Common variable for server-role system technique
bundle common server_roles_common {
  vars:
    "directiveId" string => "server-roles@@server-roles-directive@@0";
    "send_metrics" string => "no";
}
