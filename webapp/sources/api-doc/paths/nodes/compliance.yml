# SPDX-License-Identifier: CC-BY-SA-2.0
# SPDX-FileCopyrightText: 2013-2020 Normation SAS
get:
  summary: Compliance details for all nodes
  description: Get current compliance of all managed nodes
  operationId: getNodesCompliance
  parameters:
    - $ref: ../../components/parameters/compliance-level.yml
    - $ref: ../../components/parameters/compliance-percent-precision.yml
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            type: object
            required:
              - result
              - action
              - data
            properties:
              result:
                type: string
                description: Result of the request
                enum:
                  - success
                  - error
                example: success
              action:
                type: string
                description: The id of the action
                enum:
                  - getNodesCompliance
              data:
                type: object
                required:
                  - nodes
                properties:
                  nodes:
                    type: array
                    items:
                      $ref: ../../components/schemas/node-compliance.yml
  tags:
    - Nodes
  x-codeSamples:
    - lang: curl
      source:
        $ref: ../../code_samples/curl/nodes/compliance.sh
