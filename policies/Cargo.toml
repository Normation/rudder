[workspace]
members = ["rudderc", "rudder-module-type", "rudder-commons", "rudder-commons-test", "module-types/*"]
resolver = "2"

[profile.dev]
# Disabling debug info speeds up builds a bunch,
# and we don't rely on it for debugging that much.
debug = 0

[profile.release]
strip = true
