[general]
nodes_list_file = "tests/nodeslist.json"
node_id = "root"
listen = "127.0.0.1:3030"

[processing]
inventory.directory = "tests/inventories/"
inventory.output = "upstream"
inventory.catchup.frequency = 10
inventory.catchup.limit = 50
reporting.directory = "tests/runlogs/"
reporting.output = "upstream"
reporting.catchup.frequency = 10
reporting.catchup.limit = 50

[output]
database.url = "postgres://rudder:PASSWORD@127.0.0.1/rudder"
database.max_pool_size = 5
upstream.url = "https://127.0.0.1:8080"

[logging]
general.level = "info"
general.filter.level = "trace"
general.filter.nodes = []
